# import subprocess
#
# # Polecenie CMD do uruchomienia konsoli, zmiany koloru i wyświetlenia wersji Pythona
# cmd_command = 'start cmd /k "color F0 && python --version"'
#
# # Uruchomienie konsoli CMD z podanym poleceniem
# subprocess.run(cmd_command, shell=True)
